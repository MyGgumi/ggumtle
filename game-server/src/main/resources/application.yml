spring:
  application:
    name: ggumtle

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}

game:
  server:
    port: 8888
    max-players-per-room: 6
    tick-rate: 30
    id: 1

jwt:
  access:
    secret: ${ACCESS_SECRET}

logging:
  level:
    com.ggumtle: DEBUG